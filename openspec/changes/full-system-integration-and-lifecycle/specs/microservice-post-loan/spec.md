## ADDED Requirements

### Requirement: 逾期状态转移与罚息累计
系统 MUST 提供供内部调度系统触发的接口，以扫描当天到期未还款的计划，将其状态置为“逾期”，并按日复利或单利累加罚息。

#### Scenario: 每日逾期状态扫描与惩罚计算
- **WHEN** 调度系统每日触发逾期处理接口
- **THEN** 系统 MUST 找出所有已超过最后还款日且未结清的还款计划，将其状态变更为逾期，并计算出当日应增的罚息额度追加到用户的待还总额中
